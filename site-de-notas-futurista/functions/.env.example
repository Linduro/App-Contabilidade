# Copie para .env e preencha (não commitar .env)
SUPABASE_URL=https://diuudxdcemegubuajvql.supabase.co
# Chave secreta do servidor (Supabase → Project Settings → API → secret key)
# NÃO use a publishable key aqui.
SUPABASE_SECRET_KEY=
